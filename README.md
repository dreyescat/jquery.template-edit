# jQuery Template Edit

Template Edit is a jQuery plugin that turns a *textarea* into a template editor.

## Dependencies

Template Edit uses the logic-less [mustache](http://mustache.github.io/) as template rendering engine.
